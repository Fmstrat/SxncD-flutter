name: sxncd
description: Flutter plugin for incorporating SxncD into your application.
version: 0.1.0
homepage: https://sxncd.com
repository: https://github.com/Fmstrat/sxncd-flutter
topics:
  - utils
  - sync

environment:
  sdk: ^3.6.1
  flutter: ">=3.27.0"

dependencies:
  crypto: ^3.0.6
  encrypt: ^5.0.3
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
